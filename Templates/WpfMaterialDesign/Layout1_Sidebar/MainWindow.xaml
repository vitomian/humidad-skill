<Window x:Class="Layout1_Sidebar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Sidebar Layout" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <materialDesign:Card Grid.Column="0" Margin="4" Background="{DynamicResource PrimaryHueDarkBrush}">
            <StackPanel>
                <TextBlock Text="Sidebar" Foreground="White" Margin="12" />
                <Button Style="{StaticResource MaterialDesignFlatButton}" Click="Button_Click" Tag="Home">
                    <materialDesign:PackIcon Kind="Home" />
                </Button>
            </StackPanel>
        </materialDesign:Card>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <materialDesign:Card Grid.Row="0" Margin="4" Background="{DynamicResource PrimaryHueLightBrush}">
                <TextBlock Text="TopBar" />
            </materialDesign:Card>
            <materialDesign:Card Grid.Row="1" Margin="4">
                <StackPanel>
                    <TextBlock Text="MainContent" FontSize="16" FontWeight="Bold" />
                    <TextBlock Text="RegionID: R3 - MainContent" />
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>